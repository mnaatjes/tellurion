[project]
name = "tellurion-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mnaatjes", email = "michael.naatjes87@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
]

[build-system]
requires = ["uv_build>=0.11.4,<0.12.0"]
build-backend = "uv_build"
